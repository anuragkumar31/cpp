rac si shit 

#include<iostream>
#include<stdio.h>
#include<conio.h>
#include<string.h>
using namespace std;
int main()
{
    char str[50];
    gets(str);
    
    
    int j,l;
    l=strlen(str);
    
        for(j=l-1;j>=0;j--)
        {
    cout<<str[j];        
    
        }
    
    return 0;
}
